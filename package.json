{
  "name": "eslint-plugin-yacamelcase",
  "version": "1.1.0",
  "author": "kohno",
  "scripts": {
    "test": "mocha --timeout 5000 ./tests/*.js",
    "test:ci": "JUNIT_REPORT_PATH=mocha-test-results.xml mocha --colors --reporter mocha-jenkins-reporter --timeout 5000 ./tests/*.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fnwiya/eslint-plugin-yacamelcase.git"
  },
  "dependencies": {
    "eslint": "^3.14.1"
  },
  "devDependencies": {
    "mocha": "^3.2.0",
    "mocha-jenkins-reporter": "^0.3.7"
  }
}
